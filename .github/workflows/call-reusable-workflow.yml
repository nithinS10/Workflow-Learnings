# repo2: .github/workflows/call-reusable-workflow.yml
name: Call Reusable Workflow

on:
  workflow_dispatch:

jobs:
  call_reusable_workflow:
    uses: nithinS10/simple-java-maven-app/.github/workflows/reusable-workflow.yml@master
    with:
      message: "Hello from repo2!"
